<div class="side-image">
  <div class="images-wrapper"></div>
    <div class="side-image-content">
      <h4>It's All About You</h4>
        <%= form_for current_user do |f| %>
          <%= f.label :description %>
          <%= f.text_field :description, required: true %>
      <h4>Your Preferred Languages</h4>
      <ul>
      <% Language.all.each do |language| %>
        <li>
          <%= check_box_tag "language-" + language.id.to_s, language.id, current_user.languages.include?(language), class: "filled-in" %>
          <label for=<%="language-" + language.id.to_s %>> <%= language.name %></label>
       </li>
      <% end %>
      </ul>
      <%= f.submit "Update yourself. Improve yourself" %>
    <% end %>
  </div>

  </div>
</div>
